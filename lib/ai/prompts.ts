/**
 * prompts.ts — тексты промптов для AI-анализа документов и досье.
 *
 * ЭТАП 1: не реализовано (заглушка). Здесь на ЭТАПЕ 2 появятся:
 * - системный промпт для анализа отдельного документа
 *   (с требованием вернуть строгий JSON вида
 *   { document_type, fields, warnings });
 * - системный промпт для анализа всего досье
 *   (что подтверждено / чего не хватает / несоответствия /
 *   риски / рекомендации, с обязательными формулировками
 *   осторожности — "требует проверки", "возможный риск" и т.д.,
 *   и запретом на категоричные выводы).
 */

export {};

/**
 * documentAnalysis.ts — логика AI-анализа одного документа.
 *
 * ЭТАП 1: не реализовано (заглушка).
 * ЭТАП 2: здесь появится функция, которая берёт файл/текст документа,
 * формирует промпт (lib/ai/prompts.ts), вызывает callGemini
 * (lib/ai/gemini.ts) и парсит ответ в DocumentAnalysisResult.
 */

export {};
